on: [push, pull_request]
name: CI
jobs:
  build:
    name: "Build on Racket '${{ matrix.racket-version }}' (${{ matrix.racket-variant }})"
    runs-on: ubuntu-latest
    continue-on-error: ${{ matrix.experimental || false }}
    strategy:
      fail-fast: false
      matrix:
        racket-version: ["stable", "current"]
        racket-variant: ["BC", "CS"]
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
          - "pypy3.10"
          - "pypy3.11"
          - "pypy3.12"
          - "pypy3.13"
          - "pypy3.14"
        include:
          - racket-version: current
            experimental: true
    steps:
      - uses: actions/checkout@v4
      - uses: Bogdanp/setup-racket@v1.12
        with:
          architecture: x64
          distribution: full
          variant: ${{ matrix.racket-variant }}
          version: ${{ matrix.racket-version }}
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Installing rkt-pythonize and its dependencies
        run: raco pkg install --no-docs --auto --name rkt-pythonize
      - name: Compiling rkt-pythonize and building its docs
        run: raco setup --check-pkg-deps --unused-pkg-deps rkt-pythonize
      - name: Testing rkt-pythonize
        run: raco test -x -p rkt-pythonize
